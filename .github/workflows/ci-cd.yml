name: CI/CD Pipeline

on:
  push:
    branches: [main, develop,renan]
  pull_request:
    branches: [main, develop,renan]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      # 1. Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      # 3. Install dependencies
      - name: Install dependencies
        run: npm ci

      # 4. Run ESLint
      - name: Run linter
        run: npm run lint

      # 5. Run tests with Vitest and MongoMemoryServer
      - name: Run tests
        run: npm test
        env:
          NODE_ENV: test

      # 6. TypeScript build check
      - name: Build check
        run: npm run build
